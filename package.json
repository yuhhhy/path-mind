{
  "name": "path-mind",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "dev": "concurrently --kill-others-on-fail -n server,web -c magenta,cyan \"pnpm --filter server run dev\" \"wait-on http://localhost:3000/health && pnpm --filter web run dev\"",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@tanstack/eslint-plugin-query": "^5.101.1",
    "concurrently": "^10.0.3",
    "vite-plus": "catalog:",
    "wait-on": "^9.0.10"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.9.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
